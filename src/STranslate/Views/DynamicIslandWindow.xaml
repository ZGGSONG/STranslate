<Window
    x:Class="STranslate.Views.DynamicIslandWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="78"
    d:DesignWidth="400"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    ShowInTaskbar="False"
    ShowActivated="False"
    Topmost="True"
    ResizeMode="NoResize"
    Focusable="False"
    Width="400"
    Height="78"
    WindowStartupLocation="Manual"
    Loaded="OnLoaded"
    MouseLeftButtonDown="OnPillMouseLeftButtonDown"
    MouseEnter="OnPillMouseEnter"
    MouseLeave="OnPillMouseLeave">
    <Grid>
        <!-- 灵动岛胶囊主体 -->
        <Border
            x:Name="Pill"
            Margin="12"
            VerticalAlignment="Center"
            Background="#F2000000"
            BorderBrush="#40FFFFFF"
            BorderThickness="1"
            CornerRadius="27"
            Opacity="0"
            RenderTransformOrigin="0.5,0.5">
            <Border.Effect>
                <DropShadowEffect
                    x:Name="PillShadow"
                    BlurRadius="24"
                    Color="Black"
                    Opacity="0.45"
                    ShadowDepth="0" />
            </Border.Effect>
            <Border.RenderTransform>
                <ScaleTransform x:Name="PillScale" ScaleX="0.3" ScaleY="0.3" />
            </Border.RenderTransform>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- 左侧小图标（翻译标识） -->
                <Border
                    Grid.Column="0"
                    Width="28"
                    Height="28"
                    Margin="12,0,4,0"
                    VerticalAlignment="Center"
                    Background="#26FFFFFF"
                    CornerRadius="14">
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Text="译" />
                </Border>

                <!-- 翻译结果文本 -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock
                        x:Name="ResultText"
                        Margin="8,0,20,0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        FontSize="15"
                        Foreground="White"
                        Text=""
                        TextAlignment="Left"
                        TextTrimming="CharacterEllipsis"
                        TextWrapping="NoWrap" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
